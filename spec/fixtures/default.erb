HOME="<%= home %>"

for file in /etc/<%= name %>/conf.d/*; do
  if [ -f $file ]; then . $file; fi
done

for file in <%= home %>/.profile.d/*.sh; do
  if [ -f $file ]; then . $file; fi
done

<%= "tmpfile=$(#{"TMPDIR=\"#{tmpdir}\" " if tmpdir}mktemp)" %>